{
  "name": "chai-struct",
  "description": "Simple, readable, structural type comparisons for Chai",
  "version": "2.0.1",
  "main": "dist/index.js",
  "scripts": {
    "lint": "eslint src/",
    "test": "mocha -r babel-register src/*.test.js",
    "tdd": "npm t -s -- -w -R min",
    "coverage": "babel-node node_modules/.bin/babel-istanbul cover _mocha --",
    "cover": "rimraf coverage/ && run-s -s 'coverage {@}' -- src/*.test.js",
    "transpile": "rimraf dist/ && babel -d dist/ --ignore *.test.js src/",
    "precommit": "run-s -s lint cover transpile"
  },
  "files": [
    "dist/"
  ],
  "engines": {
    "node": ">=6.3.1",
    "npm": ">=3.10.3"
  },
  "keywords": [
    "object",
    "schema",
    "types",
    "chai",
    "plugin",
    "structure"
  ],
  "author": "Tim Davis",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thebearingedge/chai-struct.git"
  },
  "devDependencies": {
    "babel-cli": "6.14.0",
    "babel-eslint": "6.1.2",
    "babel-istanbul": "0.11.0",
    "babel-plugin-transform-export-extensions": "6.8.0",
    "babel-preset-node6": "11.0.0",
    "chai": "3.5.0",
    "coveralls": "2.11.12",
    "eslint": "3.5.0",
    "global": "4.3.0",
    "husky": "0.11.7",
    "mocha": "3.0.2",
    "npm-run-all": "3.1.0",
    "rimraf": "2.5.4"
  },
  "dependencies": {
    "type-diff": "3.0.1"
  }
}
